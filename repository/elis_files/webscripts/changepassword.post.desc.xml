<webscript>
  <shortname>Change Password</shortname>
  <description>Change a user's password.  This must be run as the admin user.</description>
  <url>/moodle/changepassword?username={username}&amp;password={password}</url>	
  <format default="xml" />
  <authentication>admin</authentication>
  <transaction>required</transaction>
</webscript>
